# The downloaded OpenAPI spec is never committed; only the generated output is.
openapi.yaml
dist/
node_modules/
.turbo/
.cache/
.idea
# npm pack tarballs (produced by `pnpm verify:package` / npm pack)
*.tgz
